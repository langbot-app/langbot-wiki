---
title: "スキル (Skills)"
---

Skill は **オンデマンドで起動される指示パック** です。ユーザーの記述にマッチしたとき、Agent が `activate` ツールを呼び出してその Skill の指示をコンテキストに取り込み、指示に従ってタスクを実行します。

Agent に **専門的なワークフロー、テンプレート、スクリプト** を追加するのに適しています。たとえば「Python プロジェクトを分析する」「PDF フォームに入力する」「コードレビューを行う」など。すべての指示を毎回 System Prompt に詰め込む必要はありません。

## プラグイン・MCP との違い

| やりたいこと | これを使う |
|------------|--------|
| オンデマンドで発火するワークフロー（任意でスクリプト／テンプレート付き） | Skill |
| 既存の MCP ツールサービスに接続する | MCP サーバー |
| 長期常駐機能、設定ページ、イベント監視、ナレッジベース | プラグイン |

詳しくは [ランタイムと拡張機能の関係](../sandbox/runtime-relationships) を参照してください。

## ワークフロー

```
ユーザー: 「この Python プロジェクトを分析して」
   ↓
Agent が <available_skills> から python-analyzer を発見
   ↓
Agent が activate(skill_name="python-analyzer") を呼び出し
   ↓
SKILL.md の内容が Tool Result として返される
   ↓
Agent が指示に従って実行（read / exec / write などのツールを使用）
```

Agent はマッチすると判断したときだけ Skill を起動します。未起動の Skill はコンテキストを消費しません。

## 前提条件

Skills はサンドボックスバックエンドに依存します。`box.enabled` を `true` にし、バックエンド（Docker / Nsjail / E2B）が利用可能である必要があります。Box が利用できない場合、Skill 一覧は空になり、追加／編集／起動はいずれも利用できません。

[サンドボックス設定](../sandbox/config) を参照してください。

## 最小限の Skill

Skill はディレクトリと `SKILL.md` 1 つだけです:

```
my-skill/
└── SKILL.md
```

`SKILL.md`:

```markdown
---
name: code-reviewer
display_name: コードレビューアシスタント
description: コードをレビューして改善提案を行う。ユーザーが review、レビュー、コード改善に言及したときに起動
---

## 実行手順

1. `read` で関連ファイルをざっと確認
2. 可読性、潜在的なバグ、セキュリティ問題をチェック
3. 優先度順に改善提案を提示
```

`description` を明確に書くほど、Agent はいつ起動すべきかを正確に判断できます。これが良い Skill を書くうえで最も重要なポイントです。

詳細なフィールド説明と追加のサンプルは [SKILL.md フォーマット](./skill-format) を参照してください。

## Skill を作成する

**方法 1: WebUI** — Skills ページで作成、zip アップロード、GitHub からのインポートが可能

**方法 2: サンドボックス + `register_skill`**
1. サンドボックスの `/workspace` 配下にディレクトリと `SKILL.md` を準備
2. Agent に `register_skill(path="/workspace/my-skill")` を呼び出させる

いずれの方法でも Box Runtime が管理する skill store に書き込まれます。

## パイプラインで有効化する

Skill をインストールした後、パイプライン拡張設定で Agent から見える Skill を決定する必要があります:

- 「すべての Skill を有効化」をオンにする、または
- 指定した Skill のみをバインドする

バインドは `<available_skills>` 一覧（Agent が選択できる対象）のみに影響します。完全な指示は `activate` が呼ばれた後にのみ注入されます。

## Agent が使えるツール

| ツール | 用途 |
|------|------|
| `activate` | Skill を起動し、SKILL.md をコンテキストに取り込む |
| `register_skill` | サンドボックスに用意したディレクトリを Skill として登録 |

パラメータと戻り値は [ツール API](./tools-api) を参照してください。

## よくある問題

| 症状 | 確認項目 |
|------|--------|
| Skill 一覧が空 | `box.enabled`、Box Runtime の状態、`box.local.skills_root` |
| Agent が Skill を起動しない | `description` が具体的か、パイプラインでバインドされているか、モデルが関数呼び出しに対応しているか |
| 起動後にファイルが見つからない | ファイルは Skill パッケージ内に置く必要があり、パスは `/workspace/.skills/{skill-name}/` |
| `register_skill` がエラー | パスは `/workspace` 配下である必要があり、ディレクトリまたは 2 階層以内のサブディレクトリに `SKILL.md` が必要 |

<Note>
Skills は現在、組み込み Agent 向けで、関数呼び出しとサンドボックスバックエンドに依存します。外部 Runner では各プラットフォームの同等機構を利用してください。
</Note>

## 次のステップ

- [SKILL.md フォーマット](./skill-format) — フィールド、ディレクトリ構造、執筆のヒント
- [ツール API](./tools-api) — `activate` と `register_skill` のパラメータと戻り値
